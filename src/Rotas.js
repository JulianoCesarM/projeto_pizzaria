const { BrowserRouter, Routes, Route } = require("react-router-dom");


function Rotas()
{

    return(
        <BrowserRouter>
            <Routes>
                <Route />
            </Routes>
        
        </BrowserRouter>
    );
}
export default Rotas;